class Users {
  constructor(key, username, firstName, lastName, password, email, score) {
    this.key = key;
    this.username = username;
    this.firstName = firstName;
    this.lastName = lastName;
    this.password = password;
    this.email = email;
    this.score = score;
  }
}

export default Users;
